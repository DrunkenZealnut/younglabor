<?php
/**
 * 애플리케이션 기본 설정
 * 
 * .env 파일의 환경변수를 기반으로 한 애플리케이션 기본 설정
 */

return [
    /*
    |--------------------------------------------------------------------------
    | 애플리케이션 이름
    |--------------------------------------------------------------------------
    */
    'name' => $_ENV['APP_NAME'] ?? '희망씨',

    /*
    |--------------------------------------------------------------------------
    | 애플리케이션 환경
    |--------------------------------------------------------------------------
    */
    'env' => $_ENV['APP_ENV'] ?? 'production',

    /*
    |--------------------------------------------------------------------------
    | 디버그 모드
    |--------------------------------------------------------------------------
    */
    'debug' => filter_var($_ENV['APP_DEBUG'] ?? false, FILTER_VALIDATE_BOOLEAN),

    /*
    |--------------------------------------------------------------------------
    | 애플리케이션 URL
    |--------------------------------------------------------------------------
    */
    'url' => $_ENV['APP_URL'] ?? 'http://localhost',
    'production_url' => $_ENV['PRODUCTION_URL'] ?? '',

    /*
    |--------------------------------------------------------------------------
    | 애플리케이션 보안 키
    |--------------------------------------------------------------------------
    */
    'key' => $_ENV['APP_SECRET'] ?? '',

    /*
    |--------------------------------------------------------------------------
    | 타임존
    |--------------------------------------------------------------------------
    */
    'timezone' => 'Asia/Seoul',

    /*
    |--------------------------------------------------------------------------
    | 문자셋 및 로케일
    |--------------------------------------------------------------------------
    */
    'charset' => 'UTF-8',
    'locale' => 'ko_KR',
    'fallback_locale' => 'en',

    /*
    |--------------------------------------------------------------------------
    | 보안 설정
    |--------------------------------------------------------------------------
    */
    'security' => [
        'headers' => filter_var($_ENV['SECURITY_HEADERS'] ?? true, FILTER_VALIDATE_BOOLEAN),
        'xss_protection' => filter_var($_ENV['XSS_PROTECTION'] ?? true, FILTER_VALIDATE_BOOLEAN),
        'content_type_nosniff' => filter_var($_ENV['CONTENT_TYPE_NOSNIFF'] ?? true, FILTER_VALIDATE_BOOLEAN),
    ],

    /*
    |--------------------------------------------------------------------------
    | 로그 설정
    |--------------------------------------------------------------------------
    */
    'log' => [
        'level' => $_ENV['LOG_LEVEL'] ?? 'error',
        'path' => $_ENV['LOG_PATH'] ?? 'logs',
        'file' => $_ENV['LOG_FILE'] ?? 'logs/app.log',
        'query_log' => filter_var($_ENV['ENABLE_QUERY_LOG'] ?? false, FILTER_VALIDATE_BOOLEAN),
    ],

    /*
    |--------------------------------------------------------------------------
    | 디버그 설정
    |--------------------------------------------------------------------------
    */
    'debug_options' => [
        'query' => filter_var($_ENV['QUERY_DEBUG'] ?? false, FILTER_VALIDATE_BOOLEAN),
        'template' => filter_var($_ENV['TEMPLATE_DEBUG'] ?? false, FILTER_VALIDATE_BOOLEAN),
        'error_display' => filter_var($_ENV['ERROR_DISPLAY'] ?? false, FILTER_VALIDATE_BOOLEAN),
        'performance' => filter_var($_ENV['PERFORMANCE_DEBUG'] ?? false, FILTER_VALIDATE_BOOLEAN),
    ],
];