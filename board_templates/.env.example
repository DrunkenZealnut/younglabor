# Board Templates 환경변수 설정 예시
# 이 파일을 .env로 복사하여 프로젝트에 맞게 수정하세요

# === 기본 설정 ===
# younglabor Posts 통합 테이블 사용 여부 (true/false)
BT_USE_younglabor_POSTS=true

# === 파일 업로드 설정 ===
# 파일 업로드 물리 경로 (프로젝트 루트 기준 상대 경로 권장)
# 예시:
# - 프로젝트 내부: data/file 또는 uploads
# - XAMPP 환경: /Applications/XAMPP/xamppfiles/htdocs/younglabor/data/file
# - Linux 서버: /var/www/html/younglabor/data/file
BT_UPLOAD_PATH=data/file

# 파일 업로드 URL 경로 (웹 접근 경로)
BT_UPLOAD_URL=/younglabor/data/file

# 다운로드 권한 검사 생략 여부 (true/false)
BT_DOWNLOAD_OPEN=true

# === 디버그 및 로깅 ===
# 디버그 모드 활성화 (true/false)
BT_DEBUG=false

# 로그 레벨 (DEBUG, INFO, WARNING, ERROR)
BT_LOG_LEVEL=ERROR

# === 데이터베이스 설정 (향후 확장용) ===
# BT_DB_HOST=localhost
# BT_DB_USER=root
# BT_DB_PASSWORD=
# BT_DB_DATABASE=younglabor

# === 보안 설정 (향후 확장용) ===
# BT_SECURE_MODE=false
# BT_MAX_FILE_SIZE=10485760
# BT_ALLOWED_EXTENSIONS=jpg,jpeg,png,gif,pdf,doc,docx

# === 성능 설정 (향후 확장용) ===
# BT_CACHE_ENABLED=true
# BT_CACHE_TTL=3600
# BT_MAX_POSTS_PER_PAGE=20